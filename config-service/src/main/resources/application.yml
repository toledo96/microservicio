server:
  port: 8082
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/toledo96/microservicio
          search-paths: config-data

  application:
    name: config-service
