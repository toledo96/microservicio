

  server:
    port: 8081
  spring:
    cloud:
      config:
        server:
          git:
            default-label: config-server
            uri: https://github.com/toledo96/microservicio
            searchPaths: config-data
    application:
      name: config-service
